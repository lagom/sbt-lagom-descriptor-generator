# Structure of this test:
# =======================

# In this test a hand-made lagom service consumes many downstream services specified by many swagger-json files in
# the hello-impl 'resources/' folder. Consumed services are used on a test.

> clean
> compile
> test
